<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.niuxin.mapper.ChatRecordMapper">
     <resultMap type="com.niuxin.bean.ChatRecord"  id="r">
            <result property="sendUserId" column="send_user_id"/>

          <result property="receiveUserId" column="receive_user_id"/>
             <result property="receiveGroupId" column="receive_group_id"/>
             
               <result property="imageUrl" column="image_url"/>
        <result property="audioUrl" column="audio_url"/>
          <result property="videoUrl" column="video_url"/>
             <result property="createTime" column="create_time"/>
    </resultMap>
    <parameterMap type="com.niuxin.bean.ChatRecord"  id="p"></parameterMap>
    
      <insert id="insert" parameterMap="p">    
        insert into chat_record(send_user_id,message,receive_user_id,receive_group_id,image_url,audio_url,video_url,create_time) 
        values(#{sendUserId},#{message},#{receiveUserId},#{receiveGroupId},#{"imageUrl"},#{audioUrl},#{videoUrl},#{createTime})
    </insert>
   
      <select id="selectByGroupId" resultMap="r" parameterType="int">    
        select * from chat_record where receive_group_id= #{id}
    </select>
    
         <select id="selectByUserId" resultMap="r" parameterType="int">    
        select * from chat_record where receive_user_id= #{id} or send_user_id = #{id}
    </select>

 
</mapper>